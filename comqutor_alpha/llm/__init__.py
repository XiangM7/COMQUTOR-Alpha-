"""COMQUTOR-side LLM configuration helpers that extend ``tradingagents``
without ever editing files under ``tradingagents/``.

Currently home to the DeepSeek non-thinking smoke profile (see
``deepseek_smoke.py``).
"""
